# Resume Dashboard Frontend

React 18 SPA for the Resume Dashboard.

## Requirements
- Node.js 18+
- npm

## Run
```bash
npm install
npm start
```

App runs on http://localhost:3000

Ensure the backend is running on http://localhost:8080 (proxy in package.json).
